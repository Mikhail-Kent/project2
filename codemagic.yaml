workflows:
  ios_unsigned:
    name: iOS unsigned build
    environment:
      flutter: stable
      xcode: latest
      cocoapods: default
    scripts:
      - name: "Clean project"
        script: |
          flutter clean
          rm -rf ios/Pods ios/Podfile.lock pubspec.lock
      - name: "Get Flutter packages (creates Generated.xcconfig)"
        script: |
          flutter pub get
          ls ios/Flutter || echo "Flutter folder will be generated on build"
      - name: "Run pod install (after flutter pub get)"
        script: |
          cd ios
          pod repo update
          pod install
          cd ..
      - name: "Build unsigned iOS .ipa (no codesign)"
        script: |
          # Сборка для симулятора (не требует подписи)
          flutter build ios --simulator --release

          # Создаём .ipa вручную
          mkdir -p build/ios/ipa/Payload
          cp -r build/ios/iphonesimulator/Runner.app build/ios/ipa/Payload/
          cd build/ios/ipa
          zip -r SmartAlarm.ipa Payload
      - name: "Build unsigned iOS .ipa (no codesign)"
        script: |
          # Сборка реального device-билда (без подписи)
          flutter build ios --no-codesign
          
          # Проверяем, что Runner.app собран
          if [ ! -d "build/ios/iphoneos/Runner.app" ]; then
            echo "❌ Runner.app not found — build failed."
            exit 1
          fi
          
          # Упаковываем .ipa
          mkdir -p build/ios/ipa/Payload
          cp -r build/ios/iphoneos/Runner.app build/ios/ipa/Payload/
          cd build/ios/ipa
          zip -r Runner.ipa Payload

    artifacts:
      - build/ios/ipa/*.ipa
    publishing:
      email:
        recipients:
          - your@email.com
